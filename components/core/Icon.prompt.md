Lucide icon wrapper. Load Lucide on the page and call `lucide.createIcons()` after mount.

```jsx
<Icon name="bus" size={22} color="var(--amber-600)" />
```

Masar uses Lucide (rounded, 2px stroke). Amber is reserved for live/alert icons only.
