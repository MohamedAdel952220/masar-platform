Labeled text field with focus ring, helper/error states, and RTL support.

```jsx
<Input label="Child name" placeholder="e.g. Yousef" />
<Input label="Amount" error="Required" iconLeft={<Icon name="credit-card" />} />
```
