Base surface for every panel, list item, and tile.

```jsx
<Card padding="md" interactive onClick={...}>…</Card>
<Card accent="var(--role-teacher)">Teacher note</Card>
```

- `padding`: none · sm · md · lg
- `interactive` adds a hover lift; `accent` paints a left role-stripe.
