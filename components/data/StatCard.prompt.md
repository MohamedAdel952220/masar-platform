Dashboard metric tile for manager / accountant / supervisor overviews.

```jsx
<StatCard label="Present today" value={86} unit="/ 94" delta="+4" deltaDir="up"
  icon={<Icon name="calendar-check" />} />
```

- `accent` recolors the icon tile (pass a role token).
- Numbers use tabular figures automatically.
